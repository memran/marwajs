<template>
  <h2>Feature</h2>
  <p>Demo page with per-page meta & guard.</p>
</template>

<script lang="ts">

  export const page = definePage({
    title: 'Feature Page • MarwaJS',
    middleware: [
      async ({ to }, next) => { console.log('page mw ->', to); await next(); }
    ],
    guards: [
      (to, from) => true
    ],
  });
</script>
