{
  "name": "marwajs-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/*/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm -w @marwajs/core run build && npm -w @marwajs/compiler run build",
    "dev": "npm -w playground run dev",
    "lint": "echo \"(add eslint later)\"",
    "clean": "rimraf node_modules packages/*/node_modules apps/*/node_modules packages/*/dist apps/*/dist"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
