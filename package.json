{
  "name": "marwajs",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r --filter @marwajs/* run build",
    "test": "pnpm -r --filter @marwajs/* run test"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
